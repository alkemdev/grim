# Generated by grim for {{ facts.hostname }} ({{ facts.os }}/{{ facts.arch }}).
# Active platforms (highest precedence first): {{ platforms | join(", ") }}

{% if "workstation" in platforms -%}
EDITOR=nvim
{%- else -%}
EDITOR=vim
{%- endif %}
ISOLATION={{ isolation_id }}
