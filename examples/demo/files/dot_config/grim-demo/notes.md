# grim demo

This file was placed verbatim (no templating) into `~/.config/grim-demo/notes.md`, demonstrating the
`dot_` directory convention and nested paths. The sibling `~/.demo-grimrc` is a rendered template.
